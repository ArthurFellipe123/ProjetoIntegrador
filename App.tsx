import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import Login from './scr/pages/login/BeginPage';

export default function App() {
  return (
      <Login />
  );
}

const styles = StyleSheet.create({

});
